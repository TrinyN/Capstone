import { Text, TouchableOpacity } from 'react-native'
import React from 'react'

const CustomButton = ({
    title, 
    handlePress,
}) => {
    return (
    <TouchableOpacity>

    </TouchableOpacity>
)}

export {CustomButton}